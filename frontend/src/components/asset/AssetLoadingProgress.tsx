import { Box, LinearProgress } from "@mui/material";

function AssetLoadingProgress () {
    return (
        <Box sx={{ width: '100%' }}>            
          <LinearProgress />
        </Box>
      );
}

export default AssetLoadingProgress