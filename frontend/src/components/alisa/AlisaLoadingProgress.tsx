import { Box, LinearProgress } from "@mui/material";

function AlisaLoadingProgress () {
    return (
        <Box sx={{ width: '100%' }}>            
          <LinearProgress />
        </Box>
      );
}

export default AlisaLoadingProgress