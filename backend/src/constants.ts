// constants.ts
import * as path from 'path';

export const ROOT_PATH = path.resolve(__dirname, '../../');
export const BACKEND_PATH = path.join(ROOT_PATH, 'backend');
export const MOCKS_PATH = path.join(BACKEND_PATH, 'test', 'data', 'mocks');
